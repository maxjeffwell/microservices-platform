# ===========================================
# Microservices Platform - Production Environment
# ===========================================
# Copy this file to .env.production and fill in your values
# NEVER commit .env.production to version control!

# ===========================================
# Domain & SSL
# ===========================================
# Your domain (Caddy will auto-provision SSL)
DOMAIN=api.yourdomain.com

# CORS origin (your frontend domain)
CORS_ORIGIN=https://yourdomain.com

# ===========================================
# Docker Configuration
# ===========================================
# Your Docker Hub username
DOCKER_USERNAME=your-dockerhub-username

# Docker registry (default: docker.io)
DOCKER_REGISTRY=docker.io

# Image tag to deploy (default: latest)
IMAGE_TAG=latest

# ===========================================
# Auth Service - PostgreSQL (Neon DB)
# ===========================================
# Get this from your Neon dashboard: https://console.neon.tech
AUTH_DATABASE_URL=postgresql://user:password@ep-xxx.region.aws.neon.tech/dbname?sslmode=require

# ===========================================
# JWT Configuration
# ===========================================
# Generate with: openssl rand -base64 64
JWT_SECRET=your-super-secret-jwt-key-min-32-characters

# Token expiration
JWT_EXPIRES_IN=7d
JWT_REFRESH_EXPIRES_IN=30d

# ===========================================
# Redis
# ===========================================
# Generate a strong password: openssl rand -base64 32
REDIS_PASSWORD=your-strong-redis-password

# ===========================================
# MongoDB
# ===========================================
# Credentials for MongoDB root user
MONGO_USERNAME=platform_admin
MONGO_PASSWORD=your-strong-mongo-password

# ===========================================
# InfluxDB (Analytics)
# ===========================================
INFLUXDB_USERNAME=admin
INFLUXDB_PASSWORD=your-strong-influxdb-password
INFLUXDB_ORG=microservices-platform
INFLUXDB_BUCKET=analytics

# Generate with: openssl rand -base64 48
INFLUXDB_TOKEN=your-influxdb-admin-token

# ===========================================
# Logging
# ===========================================
# Options: error, warn, info, debug
LOG_LEVEL=info

# ===========================================
# Optional: Future Services
# ===========================================
# Uncomment and configure as you add services

# User Service Database
# USER_DATABASE_URL=postgresql://user:password@host/userdb?sslmode=require

# Notification Service
# NOTIFICATION_DATABASE_URL=postgresql://user:password@host/notificationdb?sslmode=require
# SENDGRID_API_KEY=your-sendgrid-api-key
# EMAIL_FROM=noreply@yourdomain.com
# EMAIL_FROM_NAME=Your Platform Name

# Media Service (Cloudflare R2 / AWS S3)
# MEDIA_DATABASE_URL=postgresql://user:password@host/mediadb?sslmode=require
# STORAGE_PROVIDER=cloudflare-r2
# STORAGE_ACCESS_KEY=your-access-key
# STORAGE_SECRET_KEY=your-secret-key
# STORAGE_BUCKET=media
# STORAGE_ENDPOINT=https://xxx.r2.cloudflarestorage.com
